import { spawn } from "node:child_process";import { IDLE_MS, SPONSOR_COOLDOWN_MS, type Campaign } from "@9527/shared";import { ApiClient } from "./api-client.js";import { isCi,isSensitiveCommand } from "./idle-detector.js";import { readConfig, updateConfig } from "./local-config.js";import { renderSponsorCard } from "./sponsor-card.js";
export async function runWrapped(args:string[]){if(args.length===0){console.error("Usage: 9527 run -- <command>");process.exit(1)}const command=args.join(" ");const child=spawn(command,{stdio:["inherit","pipe","pipe"],shell:true});let lastOutputAt=Date.now(),lastAdAt=0,showing=false,sessionId:string|undefined;child.stdout.on("data",c=>{lastOutputAt=Date.now();process.stdout.write(c)});child.stderr.on("data",c=>{lastOutputAt=Date.now();process.stderr.write(c)});const onKey=async(buf:Buffer)=>{if(!showing)return;const k=buf.toString();if(k==="s"){showing=false;process.stdout.write("Sponsor skipped.\n")}if(k==="x"){updateConfig({adsEnabled:false});showing=false;process.stdout.write("Sponsor rewards disabled. Run 9527 enable to re-enable.\n")}if(k==="v"){try{const cfg=readConfig();const api=new ApiClient();if(!sessionId)throw new Error("No session");await api.post(`/v1/ad-sessions/${sessionId}/viewed`,{});process.stdout.write(`Sponsor link:\n${cfg.webBaseUrl}/a/${sessionId}\n\nStay for 20s to unlock points.\n`)}catch(e:any){process.stdout.write(`Could not open sponsor: ${e.message}\n`)}finally{showing=false}}};if(process.stdin.isTTY){process.stdin.setRawMode?.(true);process.stdin.resume();process.stdin.on("data",onKey)}const timer=setInterval(async()=>{const cfg=readConfig();if(showing||!cfg.adsEnabled||isCi()||isSensitiveCommand(command)||Date.now()-lastOutputAt<IDLE_MS||Date.now()-lastAdAt<SPONSOR_COOLDOWN_MS||child.exitCode!==null)return;lastAdAt=Date.now();try{const api=new ApiClient();const next=await api.post<{campaign:Campaign|null}>("/v1/campaigns/next",{deviceId:cfg.deviceId,context:{command,os:process.platform}});if(!next.campaign)return;const s=await api.post<{sessionId:string;landingUrl:string}>("/v1/ad-sessions",{campaignId:next.campaign.id,deviceId:cfg.deviceId,context:{command}});sessionId=s.sessionId;await api.post(`/v1/ad-sessions/${sessionId}/shown`,{});process.stdout.write(renderSponsorCard(next.campaign));showing=true}catch{}} ,1000);child.on("exit",code=>{clearInterval(timer);if(process.stdin.isTTY){process.stdin.off("data",onKey);process.stdin.setRawMode?.(false)}process.exit(code??0)})}
