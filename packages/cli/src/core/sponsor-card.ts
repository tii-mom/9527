import type { Campaign } from "@9527/shared";export function renderSponsorCard(c:Campaign){const cols=process.stdout.columns||80;if(cols<60)return `\n[Sponsor] ${c.sponsorName}: ${c.headline}\n[v] +${c.rewardPoints} pts / [s] skip / [x] off\n`;return `\n╭────────────────────────────────────────────╮\n│ Sponsor · ${c.sponsorName.padEnd(32).slice(0,32)} │\n│ ${c.headline.padEnd(42).slice(0,42)} │\n│ [v] View +${String(c.rewardPoints).padEnd(2)} pts   [s] Skip   [x] Off       │\n╰────────────────────────────────────────────╯\n`}
