import type { ApiResponse } from "@9527/shared";import { readConfig } from "./local-config.js";
export class ApiClient{baseUrl:string;token?:string;constructor(){const c=readConfig();this.baseUrl=c.apiBaseUrl;this.token=c.apiToken}async request<T>(path:string,init:RequestInit={}):Promise<T>{const res=await fetch(`${this.baseUrl}${path}`,{...init,headers:{"content-type":"application/json",...(this.token?{authorization:`Bearer ${this.token}`}:{}) ,...(init.headers||{})}});const json=await res.json() as ApiResponse<T>;if(!json.ok)throw new Error(json.error.message);return json.data}post<T>(p:string,b:any){return this.request<T>(p,{method:"POST",body:JSON.stringify(b)})}get<T>(p:string){return this.request<T>(p)}}
