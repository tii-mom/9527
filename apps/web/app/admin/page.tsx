async function get(path:string){try{const r=await fetch(`${process.env.NEXT_PUBLIC_API_BASE_URL||"http://localhost:3000"}${path}`,{headers:{authorization:`Bearer ${process.env.ADMIN_TOKEN||"dev-admin-token"}`},cache:"no-store"});return (await r.json()).data}catch{return {items:[]}}}export default async function Admin(){const [campaigns,items,reds,users,ledger]=await Promise.all([get("/admin/campaigns"),get("/admin/shop/items"),get("/admin/redemptions"),get("/admin/users"),get("/admin/ledger")]);return <main><h1>Admin</h1><p>Use API with Authorization: Bearer dev-admin-token to create/edit campaigns, shop items, and approve/reject redemptions.</p><h2>Campaigns</h2><pre>{JSON.stringify(campaigns.items,null,2)}</pre><h2>Shop Items</h2><pre>{JSON.stringify(items.items,null,2)}</pre><h2>Redemptions</h2><pre>{JSON.stringify(reds.items,null,2)}</pre><h2>Users</h2><pre>{JSON.stringify(users.items,null,2)}</pre><h2>Point Ledger</h2><pre>{JSON.stringify(ledger.items,null,2)}</pre></main>}
